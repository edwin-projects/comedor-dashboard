{
  "name": "comedor-petpooja",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Real-time Pet Pooja → Firebase bridge for the Comedor dashboard (Cloudflare Worker).",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
